{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tweakidea.dev/schemas/idea.json",
  "title": "Idea",
  "description": "The founder's startup idea text split into problem and solution components.",
  "type": "object",
  "required": ["text", "problem", "solution"],
  "properties": {
    "text": {
      "type": "string",
      "minLength": 1,
      "description": "Full raw idea text as submitted by the founder."
    },
    "problem": {
      "type": "string",
      "minLength": 1,
      "description": "Extracted problem statement."
    },
    "solution": {
      "type": "string",
      "minLength": 1,
      "description": "Extracted solution statement."
    },
    "codename": {
      "type": "string",
      "description": "Optional short codename for the idea (e.g. 'Airlock'). Shown in the report topbar and doc header."
    },
    "icon": {
      "type": "string",
      "description": "Optional single emoji used as the doc icon in the report. Defaults to 📋 when absent."
    },
    "subtitle": {
      "type": "string",
      "description": "Optional one-sentence framing shown under the doc title. Falls back to the first sentence of text when absent."
    }
  },
  "additionalProperties": false
}
