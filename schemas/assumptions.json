{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tweakidea.dev/schemas/assumptions.json",
  "title": "Assumptions",
  "description": "Confirmed or rejected assumptions after founder review of hypotheses.",
  "type": "array",
  "maxItems": 20,
  "items": {
    "type": "object",
    "required": ["text", "primary_dimension", "status"],
    "properties": {
      "text": {
        "type": "string",
        "minLength": 1,
        "description": "The assumption text."
      },
      "primary_dimension": {
        "type": "string",
        "description": "The evaluation dimension this assumption primarily relates to."
      },
      "status": {
        "type": "string",
        "enum": ["CONFIRMED", "UNCONFIRMED", "REJECTED"],
        "description": "Post-confirmation status — PENDING is not valid here."
      }
    },
    "additionalProperties": false
  }
}
